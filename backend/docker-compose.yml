version: "3.9"
services:
  db:
    image: postgres:16
    container_name: pcn-db
    environment:
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: pcn
    ports:
      - "5432:5432"
    volumes:
      - pcn_pgdata:/var/lib/postgresql/data

volumes:
  pcn_pgdata:
